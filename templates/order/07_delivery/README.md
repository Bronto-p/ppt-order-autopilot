# Delivery

交付文件和交付话术放在这里。

规则：

- `delivery_message.md` 必须人工确认后才能发送。
- 成品文件必须先通过 `06_qa/qa_report.md`。
- 发送前必须生成 `final_send_manifest.json`，固定联系人、消息 hash、文件路径与文件 hash。
- 发送记录必须写入 `ledgers/sent_messages.jsonl`。
